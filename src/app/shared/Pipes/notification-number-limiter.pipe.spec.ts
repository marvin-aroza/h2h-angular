import { NotificationNumberLimiterPipe } from './notification-number-limiter.pipe';

describe('NotificationNumberLimiterPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationNumberLimiterPipe();
    expect(pipe).toBeTruthy();
  });
});

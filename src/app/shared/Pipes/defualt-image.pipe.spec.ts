import { DefualtImagePipe } from './defualt-image.pipe';

describe('DefualtImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DefualtImagePipe();
    expect(pipe).toBeTruthy();
  });
});
